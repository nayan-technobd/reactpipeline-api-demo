import React from 'react';

const Error = () => {
    document.title = "Error";
    return(
        <div>
            <p>Error! This page does not exist.</p>
        </div>
    );
};

export default Error;